public class OrkutStrategy implements ISocialMediaStrategy {
  public void connectTo(String friendName) {
    System.debug('Connecting with ' + friendName + ' through Orkut [not possible though :)]');
  }
}
